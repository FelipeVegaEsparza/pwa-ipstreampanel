# Copia este archivo como `.env` para desarrollo:
#   cp .env.example .env
#
# Estos valores son el clientId/nombre por defecto SOLO en `npm run dev`.
# Para producción, cada cliente se construye con:
#   node scripts/build-client.mjs <nombre>
# que lee clients/<nombre>/client.json e inyecta el clientId en el build.

VITE_CLIENT_ID=cmtezi0ci00014raq8hrhhwfp
VITE_CLIENT_NAME=Radio Fusion Austral
